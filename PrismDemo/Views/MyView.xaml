<UserControl x:Class="PrismDemo.Views.MyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrismDemo.Views"
             xmlns:prism="http://prismlibrary.com/"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding Text}" FontSize="50" Foreground="SkyBlue" TextWrapping="Wrap"></TextBlock>
            <Button Margin="0 100 0 0" Content="接收事件" Width="100" Command="{Binding SubEventCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
